{
  "name": "zhua",
  "version": "0.0.0",
  "private": true,
  "description": "ZHUA — founding scaffold. See CONTRIBUTING.md for canonical commands.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "node --test --test-reporter=spec dist/**/*.test.js",
    "ci": "npm run lint && npm run format && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  }
}
